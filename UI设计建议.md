# 论文监控助手 — UI/UX 设计文档

> 面向科研人员的桌面应用设计参考
> 技术栈：Python + Tkinter/ttk（跨平台：macOS + Windows）

---

## 一、设计原则与定位

### 目标用户画像
- **核心用户**：高校教师、博士/硕士研究生、科研工作者
- **使用场景**：日常文献追踪、课题方向监控、期刊动态关注
- **审美偏好**：简洁克制、专业严谨、信息优先、低视觉噪音

### 设计总原则
1. **功能优先**：科研工具以效率为核心，避免过度装饰
2. **视觉降噪**：减少不必要的线条、颜色和动效
3. **信息分层**：通过间距、字重、颜色区分信息重要性
4. **一致性**：统一的间距、配色、字体规范
5. **macOS 原生感**：贴合 Apple HIG 设计语言（跨平台采用相似色系模拟）

---

## 二、整体布局架构

```
┌─ toolbar (44px, bg=#FFFFFF) ───────────────────────────┐
│  ⲎⲬ  HONGXUN · 论文发现工具          更新  说明  反馈  │
├────────────────────────────────────────────────────────┤
│ ┌─sidebar(#F1F3F5)──┐ ┌─content──────────────────────┐│
│ │ 📋 监控任务      [+]│ │ Notebook                    ││
│ │ ┌────────────────┐ │ │ [📋任务设置][📚书架]        ││
│ │ │ ▎ 任务1    ●运行│ │ │ ┌────────────────────────┐ ││
│ │ │ ▎ 任务2    ○禁用│ │ │ │ 内容区                 │ ││
│ │ └────────────────┘ │ │ └────────────────────────┘ ││
│ │ ● 每日推送运行中    │ │                            ││
│ └────────────────────┘ └─────────────────────────────┘│
├─ progress (2px idle / 展开时活跃) ─────────────────────┤
├─ status (38px, bg=#F1F3F5) ────────────────────────────┤
│ ● 就绪    状态消息...       🔒 郑州大学 v1.0.0 ⏱下次8:00│
└────────────────────────────────────────────────────────┘
```

### 布局参数

| 区域 | 说明 |
|------|------|
| **顶部工具栏** | 44px，左侧 Logo + "HONGXUN · 论文发现工具"；右侧"更新""说明""反馈" |
| **左侧侧栏** | 240px，sidebar_bg 暖灰底，卡片式任务列表 + 推送状态 |
| **右侧内容区** | ttk.Notebook 双标签：任务设置 + 文献书架 |
| **进度条** | 常驻 2px 灰色细线，检索执行时展开为带取消按钮的活跃状态 |
| **底部状态栏** | 38px，sidebar_bg 底，状态指示器 + 动态消息 + 版号 + 代码锁 + 下次执行时间 |

---

## 三、色彩系统

### 主色调（v1.3.0 暖色升级）

```
主色：    #2563EB   Tailwind Blue-600（比 Apple Blue 更沉稳）
悬停色：  #1D4ED8   Blue-700
按下色：  #1E40AF   Blue-800
选中背景： #DBEAFE   Blue-100
成功：    #16A34A   Green-600
警告：    #D97706   Amber-600
错误：    #DC2626   Red-600
```

### 背景与文字

```
页面背景：   #F8F9FA   暖白（原 #FFFFFF）
卡片背景：   #FFFFFF   纯白
侧栏背景：   #F1F3F5   暖灰（原 #F5F5F7）
边框：       #D4D6D9   暖灰边框
淡分割线：   #E5E7EB   更淡暖灰
输入框边框： #D1D5DB
标题文字：   #111827   Gray-900
正文文字：   #1F2937   Gray-800
次要文字：   #6B7280   Gray-500
占位符：     #9CA3AF   Gray-400
```

### 卡片阴影（预乘混合）

| 层级 | 颜色 | 偏移 | 透明度假算 |
|:----:|------|:----:|:----------:|
| 1 | `#ECECEF` | dy=+1 | ~8% |
| 2 | `#E7E7EA` | dy=+2 | ~6% |
| 3 | `#E2E2E5` | dy=+3 | ~4% |

### 语义标签色

| 状态 | 背景 | 文字 |
|------|------|------|
| pending（待读） | `#FEF3C7` | `#92400E` |
| read（已读） | `#DCFCE7` | `#166534` |
| excluded（排除） | `#FEE2E2` | `#991B1B` |

### 任务卡片彩色边条

| 索引 | 色值 |
|:----:|------|
| 1 | `#2563EB` (蓝) |
| 2 | `#7C3AED` (紫) |
| 3 | `#059669` (绿) |
| 4 | `#D97706` (橙) |
| 5 | `#DC2626` (红) |

---

## 四、字体系统

### 基础层级（v1.3.0 扩充）

| 层级 | 字号(mac) | 字号(win) | 字重 | 用途 |
|------|:---------:|:---------:|:----:|------|
| FONT_METRIC | 32pt | 24pt | Bold | 仪表盘数字 |
| FONT_DISPLAY | 28pt | 22pt | Bold | 大标题 |
| FONT_TITLE | 20pt | 16pt | Bold | 页面标题 |
| FONT_HEADING | 15pt | 12pt | Bold | 卡片标题 |
| FONT_BODY | 13pt | 10pt | Normal | 正文 |
| FONT_BODY_BOLD | 13pt | 10pt | Bold | 强调 |
| FONT_LABEL | 12pt | 10pt | Bold | 表单标签 |
| FONT_CAPTION | 11pt | 9pt | Normal | 辅助文字 |
| FONT_MONO | 13pt | 10pt | Normal | DOI/代码 |

### 跨平台适配

| 平台 | 字体 | 等宽字体 | 基准字号 |
|------|------|----------|:--------:|
| macOS | PingFang SC | Menlo | 13pt |
| Windows | Microsoft YaHei | Consolas | 10pt |

### 响应式缩放
- 窗口宽度变化时字号自动缩放（范围 9~19pt）
- 缩放上限 1.35x（基准 1080px 宽度）
- 防抖 150ms，5% 变化阈值

---

## 五、组件与控件

### 5.1 按钮样式

| 样式 | 底色 | 文字 | 边框 | 悬停 |
|------|------|------|------|------|
| **Primary** | `#2563EB` | 白色 | 无 | `#1D4ED8` |
| **Secondary** | 白色 | `#1F2937` | 1px `#D1D5DB` | `#F3F4F6` |
| **Danger** | 白色 | `#DC2626` | 1px 灰 | `#FEE2E2` |

所有按钮内边距：(20, 8)，大内边距营造圆润感。

### 5.2 ModernButton（自定义 Canvas 控件）

- 圆角矩形（radius=8）替代 ttk.Button 的直角
- hover 颜色过渡：6 步 180ms，`lerp_color()` 颜色插值
- press 效果：背景变 active 色，模拟 scale(0.97)

### 5.3 PlaceholderEntry（自定义 Entry）

- 1px solid `#D1D5DB` 边框，聚焦/失焦边框粗细一致
- 内置 placeholder 文字（`#9CA3AF`），聚焦自动消失
- 支持 `Cmd+A` 全选

### 5.4 Notebook 标签

| 状态 | 底色 | 文字 | 内边距 |
|------|------|------|:------:|
| 选中 | `#2563EB` | 白色 | (24, 8) |
| 未选中 | `#F1F3F5` | `#6B7280` | (24, 8) |

Notebook 整体 borderwidth=0，融入内容区。

### 5.5 RoundedCard（自定义 Canvas 控件）

- 核心组件：Canvas 绘制多层圆角矩形
- 3 层阴影（dy=+1/+2/+3，alpha 递减）
- 白底圆角（radius=12px）覆盖阴影
- 内嵌 `content` Frame 用于放置子控件
- hover 阴影抬升 2px + 加深，6 步 180ms 动画
- 所有 highlightthickness 卡片已被替代

### 5.6 StatusPill（自定义 Canvas 控件）

- pending = 琥珀底色 `#FEF3C7` + 文字 `#92400E`
- read = 绿底色 `#DCFCE7` + 文字 `#166534`
- excluded = 红底色 `#FEE2E2` + 文字 `#991B1B`
- 全圆角 (radius=11)

### 5.7 侧栏任务卡片

- 56px 高，左侧 4px 彩色边条（5 色循环）
- 任务名（FONT_BODY_BOLD）+ 状态指示（● 运行中 / ○ 已禁用）
- 选中背景 `#DBEAFE`
- 双击重命名，右键菜单（启用/禁用）
- Canvas 滚动区域（复用 Tab1 的滚动模式）

### 5.8 摘要弹窗

- 尺寸：620×580，始终最前（`-topmost`）
- 标题 + 右上角状态切换按钮
- 元数据卡片（grid 布局）：📄期刊 ✍️作者 📅日期 🔗DOI 🏷关键词
- 可滚动的 Text 控件（英文摘要 + 中文摘要），可选中复制
- 底部导航栏：◀ 上一篇 / N/N / 下一篇 ▶，↔ 方向键切换
- 底部关闭按钮 + Ctrl+W / Esc 关闭
- DOI 点击可复制

### 5.9 收件邮箱管理

- 动态添加/删除行（最多5个）
- 每行标签化显示：`primary_light` 底色 + 文字 + ✕ 删除
- 保存时自动捕获输入框中未添加的有效邮箱

### 5.10 SkeletonLoader（自定义 Canvas 控件）

- 脉冲灰条骨架屏，3 色渐变循环（`#E5E7EB` → `#F0F0F2` → `#E8E8ED`）
- 800ms 完整周期
- 用于书架数据加载时的占位

### 5.11 EmptyState（自定义 Frame）

- 居中显示：64px 大图标 + 标题 + 副标题
- 图标通过 IconCache 加载（支撑 PNG/PIL/emoji 回退）
- 标题 "文献书架是空的"，副标题 "执行检索后，论文会自动出现在这里"

---

## 六、文献书架三栏布局

### 整体结构

```
┌─ toolbar ──────────────────────────────────────────────┐
│ 状态:[全部▾] 任务:[全部▾]  [搜索...]🔍 [📤导出][↻]   │
├───────────────────┬──────────────────┬──────────────────┤
│  论文卡片列表     │  详情预览        │  元数据          │
│  (weight=1)       │  (weight=2)      │  (220px fixed)   │
│                   │                  │                  │
│  ┌──────────────┐ │  ## 论文标题     │  📖 Nature       │
│  │▎标题1        │ │  作者列表        │  🔗 10.xxx/xxx  │
│  │ 作者 et al.  │ │                  │  📅 2026-07     │
│  │ Nature       │ │  摘要全文...      │                  │
│  └──────────────┘ │                  │                  │
│  ┌──────────────┐ │  ○ 待读  📤导出  │                  │
│  │▎标题2        │ │  ← 3/25 →        │                  │
│  └──────────────┘ │                  │                  │
├───────────────────┴──────────────────┴──────────────────┤
│  总计: 127  待读: 12  已读: 3  排除: 0                  │
└────────────────────────────────────────────────────────┘
```

### 左面板 — PaperCard 列表
- 72px 高，左侧 3px 状态色条（pending/read/excluded 三色）
- 标题（1 行截断）+ 作者（1 行截断）+ 期刊名（caption）
- 单击选中 + 填充中栏 + 刷新右侧元数据
- 双击 → 弹出摘要弹窗（保留旧行为）
- **多选**：`Ctrl+Click` 切换选择，`Shift+Click` 范围选择
- 选中 ≥2 篇时出现批量操作栏

### 中栏 — QuickLookPanel
- 空格键展开/收起内联摘要预览
- 大标题 + 作者全显示 + 摘要 Text（可滚动复制）
- 操作按钮：状态循环（待读→已读→排除）+ 导出
- 前后导航：← 上一篇 / N/N / 下一篇 →
- Escape 关闭

### 右栏 — MetadataCard (RoundedCard)
- 期刊名、DOI（点击复制到剪贴板，闪烁反馈）
- 发表日期、关键词（pill 标签）
- 来源任务
- 固定 220px 宽度

### 批量操作栏
- 选中 ≥2 篇时在工具栏下方显示
- 操作：标记已读 / 标记待读 / 导出选中 / 取消选择

---

## 七、图标系统

### 架构
- 48 个 24×24 线性图标 PNG（PIL 预渲染），存放在 `client/logo/icons/`
- 两色变体：`#6B7280`（默认）+ `#2563EB`（激活）
- 运行时加载链：IconCache → 尝试 PNG → PIL 实时绘制 → emoji 回退
- 生成脚本：`tools/generate_icons.py`

### 图标清单

| 名称 | 图形 | 默认色 | 激活色 |
|------|------|:------:|:------:|
| search | 放大镜 | `#6B7280` | `#2563EB` |
| journal | 书本 | `#6B7280` | `#2563EB` |
| task | 剪贴板+勾 | `#6B7280` | `#2563EB` |
| email | 信封 | `#6B7280` | `#2563EB` |
| settings | 齿轮 | `#6B7280` | `#2563EB` |
| export | 下载箭头 | `#6B7280` | `#2563EB` |
| plus | + | `#6B7280` | `#2563EB` |
| refresh | 弧形箭头 | `#6B7280` | `#2563EB` |
| save | 软盘 | `#6B7280` | `#2563EB` |
| clock | 钟表 | `#6B7280` | `#2563EB` |
| check | ✓ | `#6B7280` | `#2563EB` |
| 其余 | — | `#6B7280` | `#2563EB` |

---

## 八、微交互

| 交互 | 实现 | 时长 |
|------|------|:----:|
| 按钮 hover | `lerp_color()` 6 步颜色插值 | 180ms |
| 按钮 press | Canvas 矩形内缩 3% 模拟 scale(0.97) | 100ms press + 150ms release |
| RoundedCard hover | 阴影偏移 3→5px + 加深 | 180ms |
| Toast 滑入 | start_y+30 → target_y，5 步 | 150ms |
| 开关切换 | thumb 位移 6 步 + 轨道颜色 lerp | 180ms |
| 骨架屏脉冲 | 3 灰阶循环（E5E7EB→F0F0F2→E8E8ED） | 800ms/cycle |
| 颜色插值 | `lerp_color()` 工具函数 | — |

---

## 九、交互行为

### 激活状态机制
- 会话级缓存：首次调用联网确认，后续瞬时返回
- 兑换成功后清除缓存，下次调用重新联网
- 需激活功能：邮件推送
- 14 天试用期内可直接使用全部功能，无需礼品券

### 检索流程
1. 配置期刊/关键词/时间范围 → 点击"执行检索"
2. 进度条从底部 2px 细线展开为活跃状态
3. 检索完成 → toast 通知（滑入动画）+ "查看书架"快捷按钮
4. 论文自动去重入库（跨任务）
5. 原 DOCX 报告照常生成

### 书架筛选流
1. 切换到书架 Tab → 三栏布局渲染
2. 状态筛选 + 任务来源 + 标题搜索
3. 单击论文 → 中栏显示详情 + 右栏元数据
4. 空格键切换 Quick Look 展开/收起
5. Ctrl/Shift 多选 → 批量操作栏
6. 双击 → 传统摘要弹窗

### 邮件发送失败交互
1. 检索完成后先保存 DOCX 附件到 `output/unsent/`
2. 发送失败时写入 `pending_email.json`
3. 邮箱设置区的「再发送」按钮可重试

### 窗口缩放
- 防抖 150ms，5% 变化阈值
- 字体和 Canvas 滚动区域自动适配
- 侧栏宽度自适应缩放

### 键盘快捷键

| 平台 | 新建任务 | 保存任务 | 摘要弹窗关闭 | 切换论文（弹窗内） |
|------|---------|---------|:----------:|:----------------:|
| macOS | `Cmd+N` | `Cmd+S` | `Cmd+W` / `Esc` | `←` `→` |
| Windows | `Ctrl+N` | `Ctrl+S` | `Esc` | `←` `→` |

---

## 十、下一步优化方向

### 布局优化
1. 检索参数区引入 RoundedCard 替代 highlightthickness 卡片
2. 搜索历史 / 预设期刊快捷填充
3. 仪表盘首页（统计大数字 + 趋势线）

### 体验提升
1. 弹窗工厂函数重构（统一 9+ 个 Toplevel 弹窗创建逻辑）
2. `query_papers` 缓存（大库书架切换更流畅）
3. 全局搜索 `Cmd+K` 浮层

### 深色模式
- 适配 macOS 系统深色模式
- 完整深色配色方案待设计

---

*本文档反映当前 UI 实际实现状态（v1.3.1），供后续迭代参考。*

## 附录：已知限制与后续修复计划

### v1.3.1 Bug 修复

| 问题 | 原因 | 修复 |
|------|------|------|
| 字体变量 None 传播 | `from-import` 在模块加载时捕获 `None`，`init_fonts()` 只改 theme 模块内全局变量，其他模块的局部绑定仍为 `None` | `init_fonts()` 末尾通过 `sys.modules` 推送到所有依赖模块 |
| `_add_receiver_widget()` 方法丢失 | 清理旧库代码时误删收件邮箱控件创建方法 | 从 git 历史恢复 |
