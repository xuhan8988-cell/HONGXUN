# 论文监控助手 — UI/UX 设计文档

> 面向科研人员的桌面应用设计参考
> 技术栈：Python + Tkinter/ttk（跨平台：macOS + Windows）

---

## 一、设计原则与定位

### 目标用户画像
- **核心用户**：高校教师、博士/硕士研究生、科研工作者
- **使用场景**：日常文献追踪、课题方向监控、期刊动态关注
- **审美偏好**：简洁克制、专业严谨、信息优先、低视觉噪音

### 设计总原则
1. **功能优先**：科研工具以效率为核心，避免过度装饰
2. **视觉降噪**：减少不必要的线条、颜色和动效
3. **信息分层**：通过间距、字重、颜色区分信息重要性
4. **一致性**：统一的间距、配色、字体规范
5. **macOS 原生感**：贴合 Apple HIG 设计语言（跨平台采用相似色系模拟）

---

## 二、整体布局架构

```
┌─ toolbar (44px, bg=#FFFFFF) ───────────────────────────┐
│  ⲎⲬ  HONGXUN                         ⬇更新  ?说明  ✉反馈│
├────────────────────────────────────────────────────────┤
│ ┌─sidebar(#EAECEF)──┐ ┌─content──────────────────────┐│
│ │ 📋 监控任务      [+]│ │ Notebook                    ││
│ │ ┌────────────────┐ │ │ [📋任务设置][📚书架]        ││
│ │ │● 任务1         │ │ │ ┌────────────────────────┐ ││
│ │ │● 任务2         │ │ │ │ 内容区                 │ ││
│ │ └────────────────┘ │ │ └────────────────────────┘ ││
│ │ ○ 推送未启动       │ │                            ││
│ └────────────────────┘ └─────────────────────────────┘│
├─ progress (2px idle / 展开时活跃) ─────────────────────┤
├─ status (38px, bg=#EAECEF) ────────────────────────────┤
│ ● 就绪    状态消息...                           v1.0.0 ⏱08:00│
└────────────────────────────────────────────────────────┘
```

### 布局参数

| 区域 | 说明 |
|------|------|
| **顶部工具栏** | 44px，左侧 Logo + "HONGXUN"；右侧图标+文字按钮（⬇更新 / ?说明 / ✉反馈） |
| **左侧侧栏** | 200px，sidebar_bg 暖灰底，紧凑卡片式任务列表 + 底部每日推送控制（集成启动/暂停/状态/下次执行时间） |
| **右侧内容区** | ttk.Notebook 双标签：任务设置（仅检索参数，无邮箱配置）+ 文献书架（Tab 选中态为浅蓝底深蓝字） |
| **进度条** | 常驻 2px 灰色细线，检索执行时展开为带取消按钮的活跃状态 |
| **底部状态栏** | 38px，sidebar_bg 底，状态指示器 + 动态消息 + 版号 + 下次执行时间 |

> 注：邮箱配置（收件邮箱、SMTP、礼品券）目前仍在任务设置 Tab 的"自动监控"卡片内。建议未来拆分为独立的第三个 Tab「⚙ 邮箱设置」，使 Tab 职责单一。

---

## 三、色彩系统

### 主色调（v1.3.2 优化版：更高对比度）

```
主色：    #2563EB   Tailwind Blue-600（比 Apple Blue 更沉稳）
悬停色：  #1D4ED8   Blue-700
按下色：  #1E40AF   Blue-800
选中背景： #DBEAFE   Blue-100
成功：    #16A34A   Green-600
警告：    #D97706   Amber-600
错误：    #DC2626   Red-600
```

### 背景与文字

```
页面背景：   #FFFFFF   纯白（原暖白 #F8F9FA）
卡片背景：   #FFFFFF   纯白
侧栏背景：   #EAECEF   暖灰（原 #F1F3F5，与页面拉大对比）
边框：       #C8CCD0   暖灰边框（原 #D4D6D9）
淡分割线：   #DEE0E3   更淡暖灰（原 #E5E7EB）
输入框边框： #D1D5DB
标题文字：   #111827   Gray-900
正文文字：   #1F2937   Gray-800
次要文字：   #6B7280   Gray-500
占位符：     #9CA3AF   Gray-400
```

### 卡片阴影（v1.3.2 简化）

| 层级 | 颜色 | 偏移 | 说明 |
|:----:|------|:----:|:----:|
| 阴影 | `#E8E8ED` | dy=+1 | 单层阴影 |
| 边框 | `#C8CCD0` | 1px | 替代原 2-3 层阴影 |

### 语义标签色

| 状态 | 背景 | 文字 |
|------|------|------|
| pending（待读） | `#FEF3C7` | `#92400E` |
| read（已读） | `#DCFCE7` | `#166534` |
| excluded（排除） | `#FEE2E2` | `#991B1B` |

### 任务卡片彩色边条

| 索引 | 色值 |
|:----:|------|
| 1 | `#2563EB` (蓝) |
| 2 | `#7C3AED` (紫) |
| 3 | `#059669` (绿) |
| 4 | `#D97706` (橙) |
| 5 | `#DC2626` (红) |

---

## 四、字体系统

### 基础层级（v1.3.0 扩充）

| 层级 | 字号(mac) | 字号(win) | 字重 | 用途 |
|------|:---------:|:---------:|:----:|------|
| FONT_METRIC | 32pt | 24pt | Bold | 仪表盘数字 |
| FONT_DISPLAY | 28pt | 22pt | Bold | 大标题 |
| FONT_TITLE | 15pt | 12pt | Bold | 页面标题 |
| FONT_HEADING | 13pt | 10pt | Bold | 卡片标题 |
| FONT_BODY | 13pt | 10pt | Normal | 正文 |
| FONT_BODY_BOLD | 13pt | 10pt | Bold | 强调 |
| FONT_LABEL | 12pt | 9pt | Bold | 表单标签 |
| FONT_CAPTION | 11pt | 8pt | Normal | 辅助文字 |
| FONT_MONO | 13pt | 10pt | Normal | DOI/代码 |

### 跨平台适配

| 平台 | 字体 | 等宽字体 | 基准字号 |
|------|------|----------|:--------:|
| macOS | PingFang SC | Menlo | 13pt |
| Windows | Microsoft YaHei | Consolas | 9pt |

### 响应式缩放
- 窗口宽度变化时字号自动缩放（范围 8~18pt）
- 缩放上限 1.35x（基准 1080px 宽度）
- 防抖 150ms，5% 变化阈值

---

## 五、组件与控件

### 5.1 按钮样式

| 样式 | 底色 | 文字 | 边框 | 悬停 |
|------|------|------|------|------|
| **Primary** | `#2563EB` | 白色 | 无 | `#1D4ED8` |
| **Secondary** | 白色 | `#1F2937` | 1px `#D1D5DB` | `#F3F4F6` |
| **Danger** | 白色 | `#DC2626` | 1px 灰 | `#FEE2E2` |

所有按钮内边距：(20, 8)，大内边距营造圆润感。

### 5.2 ModernButton（自定义 Canvas 控件）

- 圆角矩形（radius=8）替代 ttk.Button 的直角
- hover 颜色过渡：6 步 180ms，`lerp_color()` 颜色插值
- press 效果：背景变 active 色，模拟 scale(0.97)

### 5.3 PlaceholderEntry（自定义 Entry）

- 1px solid `#D1D5DB` 边框，聚焦/失焦边框粗细一致
- 内置 placeholder 文字（`#9CA3AF`），聚焦自动消失
- 支持 `Cmd+A` 全选

### 5.4 Notebook 标签

| 状态 | 底色 | 文字 | 内边距 |
|------|------|------|:------:|
| 选中 | `#DBEAFE` | `#1D4ED8` | (24, 8) |
| 未选中 | `#EAECEF` | `#6B7280` | (24, 8) |

Notebook 整体 borderwidth=0，融入内容区。

### 5.5 RoundedCard（自定义 Canvas 控件，v1.3.2 简化）

- 核心组件：Canvas 绘制圆角矩形
- 单层阴影 + 1px 边框（替代原 3 层阴影）
- 白底圆角（radius=12px）
- 内嵌 `content` Frame 用于放置子控件
- hover 阴影抬升 2px + 加深，6 步 180ms 动画
- 所有 highlightthickness 卡片已被替代

### 5.6 StatusPill（自定义 Canvas 控件）

- pending = 琥珀底色 `#FEF3C7` + 文字 `#92400E`
- read = 绿底色 `#DCFCE7` + 文字 `#166534`
- excluded = 红底色 `#FEE2E2` + 文字 `#991B1B`
- 全圆角 (radius=11)

### 5.7 侧栏任务卡片（v1.3.2 紧凑版 + 底部推送集成）

- 38px 高，选中时显示左侧 3px 蓝色边条（替代原彩色边条）
- 单行：状态点 + 任务名 + 右侧状态标签（运行中/禁用）
- 选中背景 `#DBEAFE`，默认底色 `sidebar_bg`
- 双击重命名，右键菜单（启用/禁用）
- **侧栏底部改为「每日推送」集成控制**：标题、状态（○已暂停/●运行中）、启动/暂停按钮、下次执行时间
- 推送状态从 Tab 1 的"自动监控"卡片移至侧栏底部，操作路径更短

### 5.8 摘要弹窗

- 尺寸：620×580，始终最前（`-topmost`）
- 标题 + 右上角状态切换按钮
- 元数据卡片（grid 布局）：📄期刊 ✍️作者 📅日期 🔗DOI 🏷关键词
- 可滚动的 Text 控件（英文摘要 + 中文摘要），可选中复制
- 底部导航栏：◀ 上一篇 / N/N / 下一篇 ▶，↔ 方向键切换
- 底部关闭按钮 + Ctrl+W / Esc 关闭
- DOI 点击可复制

### 5.9 收件邮箱管理

- 动态添加/删除行（最多5个）
- 每行标签化显示：`primary_light` 底色 + 文字 + ✕ 删除
- 保存时自动捕获输入框中未添加的有效邮箱

### 5.10 SkeletonLoader（自定义 Canvas 控件）

- 脉冲灰条骨架屏，3 色渐变循环（`#E5E7EB` → `#F0F0F2` → `#E8E8ED`）
- 800ms 完整周期
- 用于书架数据加载时的占位

### 5.11 EmptyState（自定义 Frame）

- 居中显示：64px 大图标 + 标题 + 副标题
- 图标通过 IconCache 加载（支撑 PNG/PIL/emoji 回退）
- 标题 "文献书架是空的"，副标题 "执行检索后，论文会自动出现在这里"

---

## 六、文献书架三栏布局

### 整体结构

```
┌─ toolbar ──────────────────────────────────────────────┐
│ 状态:[全部▾] 任务:[全部▾]  [搜索...]🔍 [📤导出][↻]   │
├───────────────────┬──────────────────┬──────────────────┤
│  Treeview 列表    │  详情预览        │  元数据          │
│  (weight=1)       │  (weight=2)      │  (220px fixed)   │
│                   │                  │                  │
│  ● 标题1          │  ## 论文标题     │  📖 Nature       │
│    作者 et al.    │  作者列表        │  🔗 10.xxx/xxx  │
│                   │                  │  📅 2026-07     │
│  ● 标题2          │  摘要全文...      │                  │
│    作者 et al.    │                  │                  │
│                   │  ● 已读  📤导出  │                  │
│                   │  ← 3/25 →        │                  │
├───────────────────┴──────────────────┴──────────────────┤
│  总计: 127  待读: 12  已读: 3  排除: 0                  │
└────────────────────────────────────────────────────────┘
```

### 左面板 — Treeview 列表
- ttk.Treeview 原生多选，无每行控件开销
- 首列状态图标（`○` 灰色=待读 / `▲` 绿色=已读 / `★` 红色=排除），形状+颜色双重区分，点击循环切换
- 单击选中 → 自动标记已读（200ms 定时器判定单击/双击），方向键切换也自动已读, 状态列点击不参与双击检测
- 双击 → 弹出摘要详情弹窗
- **多选**：`Ctrl+Click` 切换选择，`Shift+Click` 范围选择
- 选中 ≥2 篇时出现批量操作栏

### 中栏 — QuickLookPanel
- 空格键展开/收起内联摘要预览
- 大标题 + 作者全显示 + 摘要 Text（可滚动复制）
- 操作按钮：状态循环（待读→已读→排除）+ 导出
- 前后导航：← 上一篇 / N/N / 下一篇 →
- Escape 关闭

### 右栏 — MetadataCard (RoundedCard)
- 论文标题全文（Text 组件，支持选中复制）
- 期刊名、DOI（点击打开浏览器 `https://doi.org/{doi}`，闪烁反馈）
- 发表日期、关键词（pill 标签）
- 来源任务
- 摘要（Text 组件，可滚动 + 滑轨）
- 鼠标滚轮支持（可滚动模式超出高度时出现滑轨）
- 固定 220px 宽度，fill=BOTH+expand 填满高度

### 批量操作栏
- 选中 ≥2 篇时在工具栏下方显示
- 操作：标记已读 / 标记待读 / 导出选中 / 取消选择

---

## 七、图标系统

### 架构
- 48 个 24×24 线性图标 PNG（PIL 预渲染），存放在 `client/logo/icons/`
- 两色变体：`#6B7280`（默认）+ `#2563EB`（激活）
- 运行时加载链：IconCache → 尝试 PNG → PIL 实时绘制 → emoji 回退
- 生成脚本：`tools/generate_icons.py`

### 图标清单

| 名称 | 图形 | 默认色 | 激活色 |
|------|------|:------:|:------:|
| search | 放大镜 | `#6B7280` | `#2563EB` |
| journal | 书本 | `#6B7280` | `#2563EB` |
| task | 剪贴板+勾 | `#6B7280` | `#2563EB` |
| email | 信封 | `#6B7280` | `#2563EB` |
| settings | 齿轮 | `#6B7280` | `#2563EB` |
| export | 下载箭头 | `#6B7280` | `#2563EB` |
| plus | + | `#6B7280` | `#2563EB` |
| refresh | 弧形箭头 | `#6B7280` | `#2563EB` |
| save | 软盘 | `#6B7280` | `#2563EB` |
| clock | 钟表 | `#6B7280` | `#2563EB` |
| check | ✓ | `#6B7280` | `#2563EB` |
| 其余 | — | `#6B7280` | `#2563EB` |

---

## 八、微交互

| 交互 | 实现 | 时长 |
|------|------|:----:|
| 按钮 hover | `lerp_color()` 6 步颜色插值 | 180ms |
| 按钮 press | Canvas 矩形内缩 3% 模拟 scale(0.97) | 100ms press + 150ms release |
| 侧栏推送控制 | 集成在侧栏底部：状态指示 + 启动/暂停按钮 + 下次执行时间 | — |
| 示例填充 | 「🌰 试试示例」一键填入 Nature/Science/Cell + AI 关键词 | — |
| 工具栏图标 | "⬇更新" / "?说明" / "✉反馈" 图标+文字按钮（hover 变蓝色） | — |

---

## 九、交互行为

### 激活状态机制
- 会话级缓存：首次调用联网确认，后续瞬时返回
- 兑换成功后清除缓存，下次调用重新联网
- 需激活功能：邮件推送
- 14 天试用期内可直接使用全部功能，无需礼品券

### 检索流程（v1.3.2 一键检索 + 示例填充）
1. 配置期刊/关键词/时间范围 → 点击"执行检索"
2. 进度条从底部 2px 细线展开为活跃状态
3. 检索完成 → macOS 原生通知，任务设置页底部显示完成提示（不强制切换 Tab）
4. 论文自动去重入库（跨任务），DOCX 报告自动保存到 `output/` 目录
5. 新用户可通过「🌰 试试示例」一键填入 Nature/Science/Cell 预置数据

### 书架筛选流
1. 切换到书架 Tab → 三栏布局渲染
2. 状态筛选 + 任务来源 + 标题搜索
3. 单击论文 → 中栏显示详情 + 右栏元数据
4. 空格键切换 Quick Look 展开/收起
5. Ctrl/Shift 多选 → 批量操作栏
6. 双击 → 传统摘要弹窗

### 邮件发送失败交互
1. 检索完成后先保存 DOCX 附件到 `output/unsent/`
2. 发送失败时写入 `pending_email.json`
3. 邮箱设置区的「再发送」按钮可重试

### 窗口缩放
- 防抖 150ms，5% 变化阈值
- 字体和 Canvas 滚动区域自动适配
- 侧栏宽度自适应缩放

### 键盘快捷键

| 平台 | 新建任务 | 保存任务 | 摘要弹窗关闭 | 切换论文（弹窗内） |
|------|---------|---------|:----------:|:----------------:|
| macOS | `Cmd+N` | `Cmd+S` | `Cmd+W` / `Esc` | `←` `→` |
| Windows | `Ctrl+N` | `Ctrl+S` | `Esc` | `←` `→` |

---

## 十、下一步优化方向

### 下一步优化方向（v1.4.0 待办）

### 布局优化
1. **Tab 拆分**：将邮箱配置从任务设置 Tab 拆出独立为「⚙ 邮箱设置」Tab
2. 搜索历史 / 预设期刊快捷填充
3. 仪表盘首页（统计大数字 + 趋势线）

### 体验提升
1. 弹窗工厂函数重构（统一 9+ 个 Toplevel 弹窗创建逻辑）
2. `query_papers` 缓存（大库书架切换更流畅）
3. 全局搜索 `Cmd+K` 浮层

### 深色模式
- 适配 macOS 系统深色模式
- 完整深色配色方案待设计

---

*本文档反映当前 UI 实际实现状态（v1.4.0），供后续迭代参考。*

## 附录：已知限制与后续修复计划
